# Companion Protocol Documentation

This file used to contain a second copy of the protocol guide. It is retained as a compatibility link so external references do not break.

The maintained reference is [Companion Protocol & Data Layer](../documentation/ble-protocol.md). It covers BLE, USB serial, and TCP framing; current commands and responses; 1/2/3-byte path hashes; identity-scoped persistence; and fork-specific compatibility notes.

Do not add protocol details here. Update the canonical guide instead.
